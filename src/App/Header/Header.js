import React from 'react';

import Logo from './Logo/Logo';

const Header = () => {
    return (
        <header className="header row">
            <Logo/>
        </header>
    )
}

export default Header;